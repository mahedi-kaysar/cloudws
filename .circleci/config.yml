version: 2
jobs:
  build:
    docker:
      - image: ubuntu:16.04
    steps:
      - checkout
      - run:
          name: Install openjdk-8
          command: |
            apt-get -Y update
            apt-get -Y upgrade
            apt-get install -Y openjdk-8-jdk
